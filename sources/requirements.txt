# Fetcher dependencies (sources/fetch_eurlex.py).
requests>=2.31

# PDF text extraction. Load-bearing, not optional: Commission proposals are
# frequently PDF-only in Cellar (the Industrial Accelerator Act, 52026PC0100,
# has no XHTML or Formex manifestation), and those are exactly the new
# proposals the pipeline exists to catch. pypdf or poppler's pdftotext work as
# substitutes -- fetch_eurlex.pdf_to_text tries all three in that order.
pymupdf>=1.24

# Watch-agent triage (sources/watch.py). The floor is set by messages.parse()
# and the output_config parameter, which is how the classifier's verdict comes
# back as a validated object instead of prose to be regex'd.
#
# Discovery needs nothing: watch.py talks to the CELLAR SPARQL endpoint over
# urllib, so a run with --no-triage has no dependency beyond the standard
# library and can be debugged without a key.
anthropic>=0.69
pydantic>=2.7

# Coordinate conversion, and only one of them: EPSG:23700, the Hungarian
# national grid, whose double projection and ninety-metre datum shift
# sources/eov.py deliberately does not reimplement. Every other conversion here
# -- osgb36.py, utm.py -- is written and self-checked in plain Python, and the
# ruling in sources/scope.md says when that stops being honest. The schema gate
# FAILS when this is missing rather than skipping the check.
pyproj>=3.6
